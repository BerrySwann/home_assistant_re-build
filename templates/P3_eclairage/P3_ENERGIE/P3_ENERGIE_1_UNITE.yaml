# ╭──────────────────────────────────────────────────────────────────────────╮
# │ PÔLE 3. TEMPLATES : ÉNERGIE À L'UNITÉ AMHQ (kWh)                         │
# ╰──────────────────────────────────────────────────────────────────────────╯

- sensor:
    # ==========================================================================
    # CYCLE : ANNUEL
    # ==========================================================================

    # ┌────────────────────────────────────┐
    # │ PIÈCE 1. ENTRÉE                    │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_entree ---
    - name: "Hue White Lamp Entree Annuel TPL"
      unique_id: hue_white_lamp_entree_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_entree_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_entree_annuel_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 4. SALON                     │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_table_salon ---
    - name: "Hue White Lamp Table Salon Annuel TPL"
      unique_id: hue_white_lamp_table_salon_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_table_salon_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_table_salon_annuel_um') | float(0) | round(2) }}"

    # --- hue_ambiance_lamp_salon_1 ---
    - name: "Hue Ambiance Lamp Salon 1 Annuel TPL"
      unique_id: hue_ambiance_lamp_salon_1_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_ambiance_lamp_salon_1_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_ambiance_lamp_salon_1_annuel_um') | float(0) | round(2) }}"

    # --- hue_ambiance_lamp_salon_2 ---
    - name: "Hue Ambiance Lamp Salon 2 Annuel TPL"
      unique_id: hue_ambiance_lamp_salon_2_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_ambiance_lamp_salon_2_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_ambiance_lamp_salon_2_annuel_um') | float(0) | round(2) }}"

    # --- hue_ambiance_lamp_salon_3 ---
    - name: "Hue Ambiance Lamp Salon 3 Annuel TPL"
      unique_id: hue_ambiance_lamp_salon_3_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_ambiance_lamp_salon_3_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_ambiance_lamp_salon_3_annuel_um') | float(0) | round(2) }}"

    # --- hue_color_candle_salon_1 ---
    - name: "Hue Color Candle Salon 1 Annuel TPL"
      unique_id: hue_color_candle_salon_1_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_color_candle_salon_1_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_color_candle_salon_1_annuel_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 5. CUISINE                   │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_cuisine ---
    - name: "Hue White Lamp Cuisine Annuel TPL"
      unique_id: hue_white_lamp_cuisine_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_cuisine_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_cuisine_annuel_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 6. COULOIR                   │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_couloir ---
    - name: "Hue White Lamp Couloir Annuel TPL"
      unique_id: hue_white_lamp_couloir_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_couloir_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_couloir_annuel_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 7. BUREAU                    │
    # └────────────────────────────────────┘
    # --- hue_play_1_pc_bureau ---
    - name: "Hue Play 1 Pc Bureau Annuel TPL"
      unique_id: hue_play_1_pc_bureau_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_play_1_pc_bureau_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_play_1_pc_bureau_annuel_um') | float(0) | round(2) }}"

    # --- hue_play_2_pc_bureau ---
    - name: "Hue Play 2 Pc Bureau Annuel TPL"
      unique_id: hue_play_2_pc_bureau_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_play_2_pc_bureau_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_play_2_pc_bureau_annuel_um') | float(0) | round(2) }}"

    # --- hue_play_3_pc_bureau ---
    - name: "Hue Play 3 Pc Bureau Annuel TPL"
      unique_id: hue_play_3_pc_bureau_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_play_3_pc_bureau_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_play_3_pc_bureau_annuel_um') | float(0) | round(2) }}"

    # --- lampe_bureau_1_hue ---
    - name: "Lampe Bureau 1 Hue Annuel TPL"
      unique_id: lampe_bureau_1_hue_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.lampe_bureau_1_hue_annuel_um') | is_number }}"
      state: "{{ states('sensor.lampe_bureau_1_hue_annuel_um') | float(0) | round(2) }}"

    # --- lampe_bureau_2_hue ---
    - name: "Lampe Bureau 2 Hue Annuel TPL"
      unique_id: lampe_bureau_2_hue_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.lampe_bureau_2_hue_annuel_um') | is_number }}"
      state: "{{ states('sensor.lampe_bureau_2_hue_annuel_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 8. SDB                       │
    # └────────────────────────────────────┘
    # --- relais_lumiere_sdb_sonoff ---
    - name: "Relais Lumiere Sdb Sonoff Annuel TPL"
      unique_id: relais_lumiere_sdb_sonoff_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.relais_lumiere_sdb_sonoff_annuel_um') | is_number }}"
      state: "{{ states('sensor.relais_lumiere_sdb_sonoff_annuel_um') | float(0) | round(2) }}"

    # --- lampe_salle_de_bain_hue ---
    - name: "Lampe Salle De Bain Hue Annuel TPL"
      unique_id: lampe_salle_de_bain_hue_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.lampe_salle_de_bain_hue_annuel_um') | is_number }}"
      state: "{{ states('sensor.lampe_salle_de_bain_hue_annuel_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 9. CHAMBRE                   │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_chambre_1 ---
    - name: "Hue White Lamp Chambre 1 Annuel TPL"
      unique_id: hue_white_lamp_chambre_1_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_chambre_1_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_chambre_1_annuel_um') | float(0) | round(2) }}"

    # --- hue_white_lamp_chambre_2 ---
    - name: "Hue White Lamp Chambre 2 Annuel TPL"
      unique_id: hue_white_lamp_chambre_2_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_chambre_2_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_chambre_2_annuel_um') | float(0) | round(2) }}"

    # --- hue_color_candle_chambre_gege ---
    - name: "Hue Color Candle Chambre Gege Annuel TPL"
      unique_id: hue_color_candle_chambre_gege_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_color_candle_chambre_gege_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_color_candle_chambre_gege_annuel_um') | float(0) | round(2) }}"

    # --- hue_color_candle_chambre_eric ---
    - name: "Hue Color Candle Chambre Eric Annuel TPL"
      unique_id: hue_color_candle_chambre_eric_annuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_color_candle_chambre_eric_annuel_um') | is_number }}"
      state: "{{ states('sensor.hue_color_candle_chambre_eric_annuel_um') | float(0) | round(2) }}"

    # ==========================================================================
    # CYCLE : MENSUEL
    # ==========================================================================

    # ┌────────────────────────────────────┐
    # │ PIÈCE 1. ENTRÉE                    │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_entree ---
    - name: "Hue White Lamp Entree Mensuel TPL"
      unique_id: hue_white_lamp_entree_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_entree_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_entree_mensuel_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 4. SALON                     │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_table_salon ---
    - name: "Hue White Lamp Table Salon Mensuel TPL"
      unique_id: hue_white_lamp_table_salon_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_table_salon_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_table_salon_mensuel_um') | float(0) | round(2) }}"

    # --- hue_ambiance_lamp_salon_1 ---
    - name: "Hue Ambiance Lamp Salon 1 Mensuel TPL"
      unique_id: hue_ambiance_lamp_salon_1_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_ambiance_lamp_salon_1_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_ambiance_lamp_salon_1_mensuel_um') | float(0) | round(2) }}"

    # --- hue_ambiance_lamp_salon_2 ---
    - name: "Hue Ambiance Lamp Salon 2 Mensuel TPL"
      unique_id: hue_ambiance_lamp_salon_2_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_ambiance_lamp_salon_2_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_ambiance_lamp_salon_2_mensuel_um') | float(0) | round(2) }}"

    # --- hue_ambiance_lamp_salon_3 ---
    - name: "Hue Ambiance Lamp Salon 3 Mensuel TPL"
      unique_id: hue_ambiance_lamp_salon_3_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_ambiance_lamp_salon_3_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_ambiance_lamp_salon_3_mensuel_um') | float(0) | round(2) }}"

    # --- hue_color_candle_salon_1 ---
    - name: "Hue Color Candle Salon 1 Mensuel TPL"
      unique_id: hue_color_candle_salon_1_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_color_candle_salon_1_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_color_candle_salon_1_mensuel_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 5. CUISINE                   │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_cuisine ---
    - name: "Hue White Lamp Cuisine Mensuel TPL"
      unique_id: hue_white_lamp_cuisine_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_cuisine_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_cuisine_mensuel_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 6. COULOIR                   │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_couloir ---
    - name: "Hue White Lamp Couloir Mensuel TPL"
      unique_id: hue_white_lamp_couloir_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_couloir_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_couloir_mensuel_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 7. BUREAU                    │
    # └────────────────────────────────────┘
    # --- hue_play_1_pc_bureau ---
    - name: "Hue Play 1 Pc Bureau Mensuel TPL"
      unique_id: hue_play_1_pc_bureau_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_play_1_pc_bureau_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_play_1_pc_bureau_mensuel_um') | float(0) | round(2) }}"

    # --- hue_play_2_pc_bureau ---
    - name: "Hue Play 2 Pc Bureau Mensuel TPL"
      unique_id: hue_play_2_pc_bureau_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_play_2_pc_bureau_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_play_2_pc_bureau_mensuel_um') | float(0) | round(2) }}"

    # --- hue_play_3_pc_bureau ---
    - name: "Hue Play 3 Pc Bureau Mensuel TPL"
      unique_id: hue_play_3_pc_bureau_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_play_3_pc_bureau_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_play_3_pc_bureau_mensuel_um') | float(0) | round(2) }}"

    # --- lampe_bureau_1_hue ---
    - name: "Lampe Bureau 1 Hue Mensuel TPL"
      unique_id: lampe_bureau_1_hue_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.lampe_bureau_1_hue_mensuel_um') | is_number }}"
      state: "{{ states('sensor.lampe_bureau_1_hue_mensuel_um') | float(0) | round(2) }}"

    # --- lampe_bureau_2_hue ---
    - name: "Lampe Bureau 2 Hue Mensuel TPL"
      unique_id: lampe_bureau_2_hue_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.lampe_bureau_2_hue_mensuel_um') | is_number }}"
      state: "{{ states('sensor.lampe_bureau_2_hue_mensuel_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 8. SDB                       │
    # └────────────────────────────────────┘
    # --- relais_lumiere_sdb_sonoff ---
    - name: "Relais Lumiere Sdb Sonoff Mensuel TPL"
      unique_id: relais_lumiere_sdb_sonoff_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.relais_lumiere_sdb_sonoff_mensuel_um') | is_number }}"
      state: "{{ states('sensor.relais_lumiere_sdb_sonoff_mensuel_um') | float(0) | round(2) }}"

    # --- lampe_salle_de_bain_hue ---
    - name: "Lampe Salle De Bain Hue Mensuel TPL"
      unique_id: lampe_salle_de_bain_hue_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.lampe_salle_de_bain_hue_mensuel_um') | is_number }}"
      state: "{{ states('sensor.lampe_salle_de_bain_hue_mensuel_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 9. CHAMBRE                   │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_chambre_1 ---
    - name: "Hue White Lamp Chambre 1 Mensuel TPL"
      unique_id: hue_white_lamp_chambre_1_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_chambre_1_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_chambre_1_mensuel_um') | float(0) | round(2) }}"

    # --- hue_white_lamp_chambre_2 ---
    - name: "Hue White Lamp Chambre 2 Mensuel TPL"
      unique_id: hue_white_lamp_chambre_2_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_chambre_2_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_chambre_2_mensuel_um') | float(0) | round(2) }}"

    # --- hue_color_candle_chambre_gege ---
    - name: "Hue Color Candle Chambre Gege Mensuel TPL"
      unique_id: hue_color_candle_chambre_gege_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_color_candle_chambre_gege_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_color_candle_chambre_gege_mensuel_um') | float(0) | round(2) }}"

    # --- hue_color_candle_chambre_eric ---
    - name: "Hue Color Candle Chambre Eric Mensuel TPL"
      unique_id: hue_color_candle_chambre_eric_mensuel_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_color_candle_chambre_eric_mensuel_um') | is_number }}"
      state: "{{ states('sensor.hue_color_candle_chambre_eric_mensuel_um') | float(0) | round(2) }}"

    # ==========================================================================
    # CYCLE : HEBDOMADAIRE
    # ==========================================================================

    # ┌────────────────────────────────────┐
    # │ PIÈCE 1. ENTRÉE                    │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_entree ---
    - name: "Hue White Lamp Entree Hebdomadaire TPL"
      unique_id: hue_white_lamp_entree_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_entree_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_entree_hebdomadaire_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 4. SALON                     │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_table_salon ---
    - name: "Hue White Lamp Table Salon Hebdomadaire TPL"
      unique_id: hue_white_lamp_table_salon_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_table_salon_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_table_salon_hebdomadaire_um') | float(0) | round(2) }}"

    # --- hue_ambiance_lamp_salon_1 ---
    - name: "Hue Ambiance Lamp Salon 1 Hebdomadaire TPL"
      unique_id: hue_ambiance_lamp_salon_1_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_ambiance_lamp_salon_1_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_ambiance_lamp_salon_1_hebdomadaire_um') | float(0) | round(2) }}"

    # --- hue_ambiance_lamp_salon_2 ---
    - name: "Hue Ambiance Lamp Salon 2 Hebdomadaire TPL"
      unique_id: hue_ambiance_lamp_salon_2_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_ambiance_lamp_salon_2_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_ambiance_lamp_salon_2_hebdomadaire_um') | float(0) | round(2) }}"

    # --- hue_ambiance_lamp_salon_3 ---
    - name: "Hue Ambiance Lamp Salon 3 Hebdomadaire TPL"
      unique_id: hue_ambiance_lamp_salon_3_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_ambiance_lamp_salon_3_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_ambiance_lamp_salon_3_hebdomadaire_um') | float(0) | round(2) }}"

    # --- hue_color_candle_salon_1 ---
    - name: "Hue Color Candle Salon 1 Hebdomadaire TPL"
      unique_id: hue_color_candle_salon_1_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_color_candle_salon_1_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_color_candle_salon_1_hebdomadaire_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 5. CUISINE                   │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_cuisine ---
    - name: "Hue White Lamp Cuisine Hebdomadaire TPL"
      unique_id: hue_white_lamp_cuisine_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_cuisine_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_cuisine_hebdomadaire_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 6. COULOIR                   │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_couloir ---
    - name: "Hue White Lamp Couloir Hebdomadaire TPL"
      unique_id: hue_white_lamp_couloir_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_couloir_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_couloir_hebdomadaire_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 7. BUREAU                    │
    # └────────────────────────────────────┘
    # --- hue_play_1_pc_bureau ---
    - name: "Hue Play 1 Pc Bureau Hebdomadaire TPL"
      unique_id: hue_play_1_pc_bureau_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_play_1_pc_bureau_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_play_1_pc_bureau_hebdomadaire_um') | float(0) | round(2) }}"

    # --- hue_play_2_pc_bureau ---
    - name: "Hue Play 2 Pc Bureau Hebdomadaire TPL"
      unique_id: hue_play_2_pc_bureau_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_play_2_pc_bureau_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_play_2_pc_bureau_hebdomadaire_um') | float(0) | round(2) }}"

    # --- hue_play_3_pc_bureau ---
    - name: "Hue Play 3 Pc Bureau Hebdomadaire TPL"
      unique_id: hue_play_3_pc_bureau_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_play_3_pc_bureau_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_play_3_pc_bureau_hebdomadaire_um') | float(0) | round(2) }}"

    # --- lampe_bureau_1_hue ---
    - name: "Lampe Bureau 1 Hue Hebdomadaire TPL"
      unique_id: lampe_bureau_1_hue_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.lampe_bureau_1_hue_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.lampe_bureau_1_hue_hebdomadaire_um') | float(0) | round(2) }}"

    # --- lampe_bureau_2_hue ---
    - name: "Lampe Bureau 2 Hue Hebdomadaire TPL"
      unique_id: lampe_bureau_2_hue_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.lampe_bureau_2_hue_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.lampe_bureau_2_hue_hebdomadaire_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 8. SDB                       │
    # └────────────────────────────────────┘
    # --- relais_lumiere_sdb_sonoff ---
    - name: "Relais Lumiere Sdb Sonoff Hebdomadaire TPL"
      unique_id: relais_lumiere_sdb_sonoff_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.relais_lumiere_sdb_sonoff_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.relais_lumiere_sdb_sonoff_hebdomadaire_um') | float(0) | round(2) }}"

    # --- lampe_salle_de_bain_hue ---
    - name: "Lampe Salle De Bain Hue Hebdomadaire TPL"
      unique_id: lampe_salle_de_bain_hue_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.lampe_salle_de_bain_hue_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.lampe_salle_de_bain_hue_hebdomadaire_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 9. CHAMBRE                   │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_chambre_1 ---
    - name: "Hue White Lamp Chambre 1 Hebdomadaire TPL"
      unique_id: hue_white_lamp_chambre_1_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_chambre_1_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_chambre_1_hebdomadaire_um') | float(0) | round(2) }}"

    # --- hue_white_lamp_chambre_2 ---
    - name: "Hue White Lamp Chambre 2 Hebdomadaire TPL"
      unique_id: hue_white_lamp_chambre_2_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_chambre_2_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_chambre_2_hebdomadaire_um') | float(0) | round(2) }}"

    # --- hue_color_candle_chambre_gege ---
    - name: "Hue Color Candle Chambre Gege Hebdomadaire TPL"
      unique_id: hue_color_candle_chambre_gege_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_color_candle_chambre_gege_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_color_candle_chambre_gege_hebdomadaire_um') | float(0) | round(2) }}"

    # --- hue_color_candle_chambre_eric ---
    - name: "Hue Color Candle Chambre Eric Hebdomadaire TPL"
      unique_id: hue_color_candle_chambre_eric_hebdomadaire_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_color_candle_chambre_eric_hebdomadaire_um') | is_number }}"
      state: "{{ states('sensor.hue_color_candle_chambre_eric_hebdomadaire_um') | float(0) | round(2) }}"

    # ==========================================================================
    # CYCLE : QUOTIDIEN
    # ==========================================================================

    # ┌────────────────────────────────────┐
    # │ PIÈCE 1. ENTRÉE                    │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_entree ---
    - name: "Hue White Lamp Entree Quotidien TPL"
      unique_id: hue_white_lamp_entree_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_entree_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_entree_quotidien_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 4. SALON                     │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_table_salon ---
    - name: "Hue White Lamp Table Salon Quotidien TPL"
      unique_id: hue_white_lamp_table_salon_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_table_salon_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_table_salon_quotidien_um') | float(0) | round(2) }}"

    # --- hue_ambiance_lamp_salon_1 ---
    - name: "Hue Ambiance Lamp Salon 1 Quotidien TPL"
      unique_id: hue_ambiance_lamp_salon_1_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_ambiance_lamp_salon_1_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_ambiance_lamp_salon_1_quotidien_um') | float(0) | round(2) }}"

    # --- hue_ambiance_lamp_salon_2 ---
    - name: "Hue Ambiance Lamp Salon 2 Quotidien TPL"
      unique_id: hue_ambiance_lamp_salon_2_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_ambiance_lamp_salon_2_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_ambiance_lamp_salon_2_quotidien_um') | float(0) | round(2) }}"

    # --- hue_ambiance_lamp_salon_3 ---
    - name: "Hue Ambiance Lamp Salon 3 Quotidien TPL"
      unique_id: hue_ambiance_lamp_salon_3_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_ambiance_lamp_salon_3_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_ambiance_lamp_salon_3_quotidien_um') | float(0) | round(2) }}"

    # --- hue_color_candle_salon_1 ---
    - name: "Hue Color Candle Salon 1 Quotidien TPL"
      unique_id: hue_color_candle_salon_1_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_color_candle_salon_1_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_color_candle_salon_1_quotidien_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 5. CUISINE                   │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_cuisine ---
    - name: "Hue White Lamp Cuisine Quotidien TPL"
      unique_id: hue_white_lamp_cuisine_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_cuisine_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_cuisine_quotidien_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 6. COULOIR                   │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_couloir ---
    - name: "Hue White Lamp Couloir Quotidien TPL"
      unique_id: hue_white_lamp_couloir_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_couloir_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_couloir_quotidien_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 7. BUREAU                    │
    # └────────────────────────────────────┘
    # --- hue_play_1_pc_bureau ---
    - name: "Hue Play 1 Pc Bureau Quotidien TPL"
      unique_id: hue_play_1_pc_bureau_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_play_1_pc_bureau_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_play_1_pc_bureau_quotidien_um') | float(0) | round(2) }}"

    # --- hue_play_2_pc_bureau ---
    - name: "Hue Play 2 Pc Bureau Quotidien TPL"
      unique_id: hue_play_2_pc_bureau_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_play_2_pc_bureau_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_play_2_pc_bureau_quotidien_um') | float(0) | round(2) }}"

    # --- hue_play_3_pc_bureau ---
    - name: "Hue Play 3 Pc Bureau Quotidien TPL"
      unique_id: hue_play_3_pc_bureau_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_play_3_pc_bureau_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_play_3_pc_bureau_quotidien_um') | float(0) | round(2) }}"

    # --- lampe_bureau_1_hue ---
    - name: "Lampe Bureau 1 Hue Quotidien TPL"
      unique_id: lampe_bureau_1_hue_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.lampe_bureau_1_hue_quotidien_um') | is_number }}"
      state: "{{ states('sensor.lampe_bureau_1_hue_quotidien_um') | float(0) | round(2) }}"

    # --- lampe_bureau_2_hue ---
    - name: "Lampe Bureau 2 Hue Quotidien TPL"
      unique_id: lampe_bureau_2_hue_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.lampe_bureau_2_hue_quotidien_um') | is_number }}"
      state: "{{ states('sensor.lampe_bureau_2_hue_quotidien_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 8. SDB                       │
    # └────────────────────────────────────┘
    # --- relais_lumiere_sdb_sonoff ---
    - name: "Relais Lumiere Sdb Sonoff Quotidien TPL"
      unique_id: relais_lumiere_sdb_sonoff_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.relais_lumiere_sdb_sonoff_quotidien_um') | is_number }}"
      state: "{{ states('sensor.relais_lumiere_sdb_sonoff_quotidien_um') | float(0) | round(2) }}"

    # --- lampe_salle_de_bain_hue ---
    - name: "Lampe Salle De Bain Hue Quotidien TPL"
      unique_id: lampe_salle_de_bain_hue_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.lampe_salle_de_bain_hue_quotidien_um') | is_number }}"
      state: "{{ states('sensor.lampe_salle_de_bain_hue_quotidien_um') | float(0) | round(2) }}"

    # ┌────────────────────────────────────┐
    # │ PIÈCE 9. CHAMBRE                   │
    # └────────────────────────────────────┘
    # --- hue_white_lamp_chambre_1 ---
    - name: "Hue White Lamp Chambre 1 Quotidien TPL"
      unique_id: hue_white_lamp_chambre_1_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_chambre_1_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_chambre_1_quotidien_um') | float(0) | round(2) }}"

    # --- hue_white_lamp_chambre_2 ---
    - name: "Hue White Lamp Chambre 2 Quotidien TPL"
      unique_id: hue_white_lamp_chambre_2_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_white_lamp_chambre_2_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_white_lamp_chambre_2_quotidien_um') | float(0) | round(2) }}"

    # --- hue_color_candle_chambre_gege ---
    - name: "Hue Color Candle Chambre Gege Quotidien TPL"
      unique_id: hue_color_candle_chambre_gege_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_color_candle_chambre_gege_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_color_candle_chambre_gege_quotidien_um') | float(0) | round(2) }}"

    # --- hue_color_candle_chambre_eric ---
    - name: "Hue Color Candle Chambre Eric Quotidien TPL"
      unique_id: hue_color_candle_chambre_eric_quotidien_tpl
      unit_of_measurement: "kWh"
      device_class: energy
      state_class: total_increasing
      availability: "{{ states('sensor.hue_color_candle_chambre_eric_quotidien_um') | is_number }}"
      state: "{{ states('sensor.hue_color_candle_chambre_eric_quotidien_um') | float(0) | round(2) }}"
