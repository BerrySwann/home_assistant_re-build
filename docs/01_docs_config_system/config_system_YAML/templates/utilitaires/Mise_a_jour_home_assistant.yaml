# ╭──────────────────────────────────────────────────────────────────────────╮
# │ TEMPLATE : CAPTEUR NOMBRE DE MISES À JOUR DISPONIBLES (HA + ADD-ONS)     │
# ╰──────────────────────────────────────────────────────────────────────────╯
#
# ## 📝 DESCRIPTION :
# Calcule le nombre total de mises à jour disponibles dans Home Assistant
# (Core, HACS, add-ons, intégrations...) en comptant les entités du domaine
# "update" dont l'état est "on" (= mise à jour en attente).
#
# ## 🧮 CALCUL & SOURCES :
# - Source : domaine "update" (toutes entités update.*)
# - Filtre : state == 'on'  →  mise à jour disponible
# - Résultat : entier (0 = tout à jour, N = N mises à jour en attente)
#
#
# ## 🔗 CHAÎNE DE DÉPENDANCES :
#
# | MATÉRIEL       | CAPTEUR BRUT (source)                              | CE FICHIER (unique_id)                             | AVAL                               |
# |:---------------|:---------------------------------------------------|:---------------------------------------------------|:-----------------------------------|
# | HA natif (update) | update.home_assistant_core_update / supervisor     | ha_update_disponible / ha_version_actuelle         | L4C3 Mises à jour HA               |
#
# ## ⚠️ IMPORTANT (PIÈGES) :
# - Ne pas utiliser "states.update" si certaines entités update.* sont
#   "unavailable" — le filtre selectattr gère correctement ce cas.
# - L'entité générée est sensor.available_updates (unique_id snake_case).
#
# ## 🖥️ TABLEAU DE BORD (VIGNETTES PRINCIPALES) :
# - Sert à la remontée d'info pour la vignette : L4C3 Mises à jour HA

- sensor:
    # --- available_updates ---
    - name: "Available Updates"
      unique_id: available_updates
      icon: mdi:update
      state: >
        {{
          states.update
          | selectattr('state', 'eq', 'on')
          | list
          | count
        }}

# annotations_log:
# - [L1-3]   Ajout boîte ASCII arrondie 74 car. (titre principal obligatoire).
# - [L5-17]  Ajout en-tête de documentation (Description, Calcul, Pièges, Vignette).
# - [L19]    Ajout titre tertiaire (slug unique_id).
# - [L20-28] Sensor inchangé — logique fonctionnelle conservée.
