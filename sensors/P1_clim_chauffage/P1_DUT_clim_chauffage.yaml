# ╭──────────────────────────────────────────────────────────────────────────╮
# │ P1. SENSORS — DURÉE D'UTILISATION TOTALE [DUT] CHAUFFAGE & CLIM         │
# ╰──────────────────────────────────────────────────────────────────────────╯
#
# ## 📝 DESCRIPTION :
# Capteurs history_stats mesurant le temps de fonctionnement quotidien (DUT)
# des 6 équipements P1. Remise à zéro à minuit chaque jour.
# Unité : heures (type: time).
#
# ## 🧮 CALCUL & SOURCES :
# - Salon / Cuisine / Bureau / Chambre : surveille sensor.*_power_status → state "on"
#   (source : templates/P1_clim_chauffage/ui_dashboard/ui_dashboard.yaml)
# - SdB Soufflant   : surveille sensor.sdb_soufflant_etat → state "Heat"
# - SdB Sèche-Serv. : surveille sensor.sdb_seche_serviette_etat → state "Heat"
#   (sources : templates/P1_clim_chauffage/ui_dashboard/ui_dashboard.yaml)
#
# ## ⚠️ IMPORTANT (PIÈGES) :
# - Les capteurs *_power_status (Salon/Cuisine/Bureau/Chambre) sont des status
#   textuels ("on" / "off") — bien utiliser state: "on" (pas "On").
# - Les capteurs SdB utilisent l'état du mode clim ("Heat") → sensible à la casse.
# - Ce fichier produit : sensor.dut_clim_salon, sensor.dut_radiateur_cuisine,
#   sensor.dut_clim_bureau, sensor.dut_sdb_soufflant,
#   sensor.dut_sdb_seche_serviettes, sensor.dut_clim_chambre
# - sensor.dut_sdb_total (somme soufflant + sèche-serv.) est un capteur template
#   → défini dans templates/P1_clim_chauffage/P1_TOTAL/P1_DUT_TOTAL.yaml
# - Ce fichier va dans sensors/ (platform: history_stats) — PAS dans templates/.
#
# ## 🖥️ TABLEAU DE BORD (VIGNETTES PRINCIPALES) :
# - Sert à la vignette L2C2 (Conso Énergie Clim / Radiateur / Soufflant)
# - Sert aux graphes apexcharts "D.U.T. & Performance" par pièce (page energie-clim)

# ┌─────────────────────────────────────┐
# │ 4. SALON                            │
# └─────────────────────────────────────┘

# --- dut_clim_salon ---
- platform: history_stats
  name: "DUT Clim Salon"
  unique_id: dut_clim_salon
  entity_id: sensor.salon_power_status
  state: "on"
  type: time
  start: "{{ now().replace(hour=0, minute=0, second=0, microsecond=0) }}"
  end: "{{ now() }}"

# ┌─────────────────────────────────────┐
# │ 5. CUISINE                          │
# └─────────────────────────────────────┘

# --- dut_radiateur_cuisine ---
- platform: history_stats
  name: "DUT Radiateur Cuisine"
  unique_id: dut_radiateur_cuisine
  entity_id: sensor.cuisine_power_status
  state: "on"
  type: time
  start: "{{ now().replace(hour=0, minute=0, second=0, microsecond=0) }}"
  end: "{{ now() }}"

# ┌─────────────────────────────────────┐
# │ 7. BUREAU                           │
# └─────────────────────────────────────┘

# --- dut_clim_bureau ---
- platform: history_stats
  name: "DUT Clim Bureau"
  unique_id: dut_clim_bureau
  entity_id: sensor.bureau_power_status
  state: "on"
  type: time
  start: "{{ now().replace(hour=0, minute=0, second=0, microsecond=0) }}"
  end: "{{ now() }}"

# ┌─────────────────────────────────────┐
# │ 8. SDB                              │
# └─────────────────────────────────────┘

# --- dut_sdb_soufflant ---
- platform: history_stats
  name: "DUT SdB Soufflant"
  unique_id: dut_sdb_soufflant
  entity_id: sensor.sdb_soufflant_etat
  state: "Heat"
  type: time
  start: "{{ now().replace(hour=0, minute=0, second=0, microsecond=0) }}"
  end: "{{ now() }}"

# --- dut_sdb_seche_serviettes ---
- platform: history_stats
  name: "DUT SdB Sèche-Serviettes"
  unique_id: dut_sdb_seche_serviettes
  entity_id: sensor.sdb_seche_serviette_etat
  state: "Heat"
  type: time
  start: "{{ now().replace(hour=0, minute=0, second=0, microsecond=0) }}"
  end: "{{ now() }}"

# ┌─────────────────────────────────────┐
# │ 9. CHAMBRE                          │
# └─────────────────────────────────────┘

# --- dut_clim_chambre ---
- platform: history_stats
  name: "DUT Clim Chambre"
  unique_id: dut_clim_chambre
  entity_id: sensor.chambre_power_status
  state: "on"
  type: time
  start: "{{ now().replace(hour=0, minute=0, second=0, microsecond=0) }}"
  end: "{{ now() }}"

# annotations_log:
# [Création] Nouveau fichier — absent du dépôt GitHub ReBuild à la date 2026-03-22.
# [Source]   Basé sur ancien dépôt sensors/p1_1_sensors - dut.yaml (BerrySwann/home-assistant-config).
# [L36-42]   Salon    : entity sensor.salon_power_status, state "on"
# [L51-57]   Cuisine  : entity sensor.cuisine_power_status, state "on"
# [L66-72]   Bureau   : entity sensor.bureau_power_status, state "on"
# [L81-87]   Soufflant SdB : entity sensor.sdb_soufflant_etat, state "Heat"
# [L90-96]   Sèche-Serv. SdB : entity sensor.sdb_seche_serviette_etat, state "Heat"
# [L105-111] Chambre  : entity sensor.chambre_power_status, state "on"
